# FeedHacker — session state

**This file is the handoff between sessions.** A session has no memory of the last one; this
file *is* that memory. Two rules make it work, and both are standing rules in
[`CLAUDE.md`](CLAUDE.md):

> **Every session STARTS here** — read this file top to bottom, then run the Startup checklist.
> **Every session ENDS here** — before signing off, run the Close-out checklist below.

Companion records: [`RELEASES.md`](RELEASES.md) (per-version ship record) ·
[`CHANGELOG.md`](CHANGELOG.md) (what changed) · [`KNOWN_ISSUES.md`](KNOWN_ISSUES.md) (bugs and
their fixes) · [`best_practices.md`](best_practices.md) (the coding standard) ·
[`TEST_MATRIX.md`](TEST_MATRIX.md) (what a change obliges you to run).

---

## 1. Open items — READ FIRST

The single list of what is still outstanding. Nothing else in this file is a to-do; if it is not
here, it is not open. Close an item by deleting its row and saying so in the Session log.

| # | Open item | Who | Detail |
|---|---|---|---|
| 1 | **Three store/landing screenshots still show Solo — DEFERRED by the maintainer, not blocking** | maintainer (when they want it) | `store/screenshot-1-mixer.png` and `docs/carousel/carousel-2-mixer.png` show the popup with **M and S** buttons; `docs/carousel/carousel-5-transparent.png` shows the options Actions card with the **Solo** row. **Raised before the 2026-09-16 store upload and consciously deferred:** *“we'll make changes to the store carousel if we need to. this is part of our text to resolve the latest issues.”* 0.8.0 was uploaded with these images in place. **Do not re-raise this as a blocker** — it is a known, accepted cosmetic gap. **Status changed 2026-09-17, without changing the priority:** solo is now permanently retired (§1 item 9), so these images advertise a feature that no longer exists and is not coming back, rather than one that is temporarily away. That is a stronger reason to regenerate them eventually and still not a reason to block anything. Listing assets are an **independent publish channel** from the package (FH-033), so they can be corrected at any time without cutting a new version. **There is no screenshot generator in the repo** — the 2026-08-21 set was captured ad hoc from the built extension via the `test/system` harness and composed in the brand style, so regenerating is real work, not an edit. `store/screenshot-2-detection.png`, `-3-stub.png` and carousel 1/3/4 show the feed and stubs and are unaffected. |
| 2 | **The FH-049 measurement is DONE — and it found a second cause, FH-060, now fixed** | closed once 0.9.0 is on the maintainer's machine | The 2026-09-17 export was the first valid one: `version: 0.8.0`, all 300 entries dated that day, calibration fresh with `labels: 0` (so the reset landed). It read **300 decisions over 5 distinct posts** (87/87/86/26/14), median gap **1548ms identical across the top three**, and **189 observations → 12 distinct feature vectors** (×14–29). Because the vectors were byte-identical repeats the scored **text never changed**, which ruled out post identity and pointed at the scheduler: **FH-060**, a tab re-applying the feed on its own tally write. Fixed in 0.9.0 with unit+integration+system tests (system: 12 decisions over 3 posts in a 7s dwell before the fix, 3 after). **What is left:** once 0.9.0 is installed, one more **Reset AI-slop learning** + browse + **Export log (JSON)** to confirm on the live feed that decisions ≈ distinct posts and the threshold stops climbing. |
| 3 | **The mis-tuned model repairs ITSELF on update as of 0.9.0 — nothing for the maintainer to do** | closed on install of 0.9.0 | Measured from the 2026-09-17 export: auto-calibration, fed 189 sightings of 12 posts, read `broetry` as firing on **76.75%** of the population, damped it **1.400 → 0.792** and `bullets` **0.900 → 0.516**, and pushed the threshold **0.500 → 0.570** — a model tuned to catch *less*. The FH-060 code fix could not undo it (weights and threshold are stored user state and survive an update), which is why the maintainer asked for it to be automatic: *"make .9 upon install perform the reset once during the install so users in the wild don't have to be told to do it."* **Done:** the service worker now runs that reset itself on update, once, keyed in a `feedhacker:migrations` ledger, and banked as done on a fresh install so a first update cannot wipe a legitimately trained model. Clears only the AI; mute settings, authors, custom filters and display choices untouched. **The manual button is no longer a prerequisite for anything** — keep it in mind only if a future export shows the threshold climbing again. |
| 4 | **Settled: the AI-slop model does NOT key on the topic “AI”** | — (informational) | Asked by the maintainer 2026-09-15: *“it shouldn't block because someone just uses the term AI — it should be looking for the algorithm related to a post being generated by AI, not talking about AI.”* **Already true, and measured on their own 63-post capture:** 28 posts mention AI, 35 do not; mean slop probability **0.168 for both**, and **0 of 63** would be hidden as slop at the shipped threshold. `scorer.ts` has no topic features at all — every tell is structural (connective density, sentence-length uniformity, banlist phrasing, emoji, broetry) — and `claudisms.json` contains **zero** occurrences of `ai`, `chatgpt`, `gpt`, `llm`, `artificial intelligence`, `openai` or `copilot`. **Caveat:** that measurement used `defaultWeights()`. A user whose model self-tuned during the FH-049 duplicate flood could still behave differently — and **Reset AI-slop learning genuinely restores the shipped algorithm only from 0.6.0** (FH-054). If AI-topic posts are ever reported as hidden, get the decision log (item 1) before changing the scorer. |
| 5 | **LinkedIn no longer exposes the activity URN — post identity now rests on the text hash alone** | next session (informational) | Settled 2026-09-15 by inspecting two live home-feed captures: there is **no `data-urn`, no `data-id` and no `role="article"` anywhere on the page**, and `postKey()` fell back to `textHash` for **42 of 42** posts. The id is still recoverable, but only sparsely — 3 posts carried a `/feed/update/urn:li:…` permalink and 5 a `replaceableComment_urn:li:comment:(activity:<postid>,…)` componentkey. **Deliberately not harvested**: the permalink route can pick up the *original* post's id inside a reshare and collide two feed entries onto one verdict, which is worse than the hash. Revisit only if the decision log in item 1 shows identity actually slipping. |
| 9 | **SETTLED 2026-09-17: solo is permanently RETIRED. Not deferred, not on the roadmap — do not propose rebuilding it** | — (informational, closed) | The maintainer's words, after being shown the evidence below: *“ok so solo is permanently retired.”* **Keep the two steps distinct, because conflating them is how a record misleads.** Solo was **removed** in 0.8.0 for a practical reason (LinkedIn's redesign broke post identity and furniture detection, and solo sat before the scorer, so while it was on the model never ran and the decision log froze — removing it restored the instruments). It was **retired** a day later as a product decision, on three pieces of evidence: (1) **every** “FeedHacker is hiding everything” report this project ever received traced to solo and **none** traced to the AI, and that misdirection cost three rounds of work on the model; (2) the mitigation was **already tried and falsified** — 0.5.0 named the mode on every stub and added a one-click “Show everything” exit on every row and group summary, and three further reports followed, which means FH-059's remaining return-condition had been tested and failed before it was written down; (3) solo's *correct* output routinely looked broken — on the maintainer's own 63-post capture, solo = Hiring matched exactly **one** post. Plus it re-imports a bug class: inverting a subtractive filter into a whitelist is why FH-056 (an author mute silently outranking solo) could exist at all. **Roadmap FH-059 is closed as retired, not as done.** **Nothing replaces it, by the maintainer's call** — *“I don't think options would be needed. there is the log in the advanced settings already.”* Correct: the AI-slop decision log already answers “what did this catch and why” per post, with probabilities, the tells that fired and a JSON export. **One precise, tiny gap if it ever matters** (not a task): `history[day].byId` already stores per-filter hide counts, and the Insights panel renders only `total`, so the deterministic filters have counts in storage that nothing displays. |
| 10 | **All four of the maintainer's 2026-09-17 UX requests are DONE — one thing left to check on the live feed** | maintainer (confirm on 0.9.0) | **(a) Grouping — DONE (FH-061):** rows now fold by reason, not adjacency. **(b) The group splat — DONE by (a):** a slop row is all slop, so the splat covers every member. **(c) Marking a shown post as slop — DONE (FH-062):** a low-weight splat on posts FeedHacker shows; clicking trains **label 1** and hides the post, with Show anyway as the undo. Suppressed on fragments, furniture, and with the filter off; the feature vector rides the verdict ledger so it survives LinkedIn's re-renders with no re-scoring. **(d) Learning both ways — DONE:** the negative half already worked (Show anyway → label 0); (c) supplied the positive half. **Still to verify on the live feed:** the click guard is **reactive** — `onInteract()` fires from *inside* the click handler, so the first click in a while is unprotected. FH-060 removed the 1.5s stub rebuild that made that bite constantly, so it may simply be gone. **Ask the maintainer whether clicks still need repeating on 0.9.0 before changing it** — a preventive guard (pointerdown, and never rebuilding a stub containing the focused element) is the fix if it persists. |
| 11 | **Confirm 0.9.0 publishes on the store** | next session | **Uploaded and submitted 2026-09-17 22:09:02 UTC** (run #28). `Publish successful` means **submitted for Google review, NOT approved**. **Check the store listing**, never the inbox: `https://chromewebstore.google.com/detail/feedhacker/kccajfoghkplakndamlohpepopdpelkb`. Live only when it reads *Version 0.9.0*. `WebFetch` caches 15 min per URL — **vary the URL** and say which you used (`?hl=en-GB`, `?hl=en-CA`, `?hl=en-AU`, `?hl=en-NZ`, `?hl=en-IE` are used up). Review took ~14 min for 0.6.0, ~30 for 0.7.0, and overnight for 0.8.0 — **slow is not rejected**, and the last wait does not bound the next. ⚠️ **Do not cut another store release while this is pending.** With the publisher id now working, the blind `cancelSubmission` would **succeed** and silently withdraw 0.9.0 from review; pass **`cancel_pending: false`** if a re-upload of 0.9.0 itself is ever genuinely needed. When Live: mark the 0.9.0 store column **Live** in `RELEASES.md` with the listing's date, set §2 latest-on-the-store and confirmed-live to 0.9.0 and the slot to OPEN, and close this item. |
| 7 | **Windows sideload users must re-install once** | maintainer (now partly self-serving) | FH-042. The 0.4.5 updater cannot deliver its own fix, so an affected user must re-run `installer\install.bat` from a current `feedhacker-<version>-win.zip`. **0.6.0 ships a release note saying exactly this** (`CHANGELOG.md`, which `store/README.md` makes the source of the store's release notes), so anyone who reads the notes is told. It stays open because the people who most need it are precisely the ones **not receiving updates** — a note in a release they never get cannot reach them. Closing it needs a channel they still see (the repo README / releases page, or a direct message), not another release note. |

## 2. Current state

| | |
|---|---|
| **Latest version** | **0.9.0** — on GitHub since 2026-09-17 22:09:24 UTC (tag `v0.9.0` @ `f56d393`), and **uploaded to the store 22:09:02 UTC** (run #28). Both destinations in one run |
| **Latest on the store** | **0.8.0 is still the LIVE version.** 0.9.0 is **submitted and awaiting Google review** — `Publish successful` means submitted, not approved. Store users get it only once Google publishes it (§1 item 11) |
| **Previous** | 0.8.0 — published 2026-09-17 (listing: “Updated 17 September 2026”), ~overnight after its 2026-09-16 20:01 upload |
| **Dev cycle** | **0.9.0 SHIPPED** (GitHub + store submitted) on the maintainer's “ship it”, 2026-09-17. **0.10.0 not started** — bump `manifest.json`/`package.json` to 0.10.0 and open a `## [0.10.0] — unreleased` section on the next change. **Do not release again without an explicit “ship”.** |
| **Store item** | `kccajfoghkplakndamlohpepopdpelkb` |
| **Confirmed live on the store** | **0.8.0** — confirmed 2026-09-17 from the store listing (see the row above). *Previously 0.7.0*, confirmed 2026-09-15 two ways: the **store listing** ("Version 0.7.0, Updated September 15, 2026") and Google's publish email (19:54:24 UTC). *(This row read 0.5.0 until 2026-09-16; it had simply not been updated when 0.6.0 and 0.7.0 were each confirmed live. The listing is the authority — see `RELEASES.md`.)* |
| **How to check what is live** | **Read the store listing** — `https://chromewebstore.google.com/detail/feedhacker/kccajfoghkplakndamlohpepopdpelkb`. It is the authority and it is never ambiguous. **Correction (2026-09-15 20:18):** this row briefly claimed the item “publishes silently, three for three”. **That was wrong.** 0.6.0 and 0.7.0 both DID email (18:24:40 and 19:54:24 UTC). The 0.6.0 message sat in **Trash**, and `newer_than:2d` without `in:anywhere` does not search Trash — so an absent result proved nothing. **0.4.6 and 0.5.0 genuinely had no email** (that search did use `in:anywhere`). So: the email is a **fine positive signal**; its absence means nothing **unless you searched `in:anywhere`**, and even then prefer the listing. |
| **Submission slot** | **BLOCKED** — 0.9.0 submitted 2026-09-17 22:09:02 UTC, pending review (§1 item 11). ⚠️ The blind `cancelSubmission` now **works** (the publisher id is fixed and proven), so a new store release would silently withdraw 0.9.0 and restart its review. GitHub releases are never blocked — only the store upload. Re-uploading 0.9.0 itself needs **`cancel_pending: false`**. |
| **Dev branch** | the harness assigns a per-session `claude/*` branch; reset it from `origin/main` for each change |
| **Review** | no AI reviewer runs on this repo — **green CI is the merge gate** (`REVIEWERS_STATUS.md`) |

## 3. Startup checklist (run every new session)

1. **Read this file** — §1 Open items, then §2 Current state, then §6 Key facts.
2. **Skim the record** — `RELEASES.md`, then the top of `CHANGELOG.md`.
3. **Check what is LIVE on the Chrome Web Store — from the LISTING, not the inbox.** Load
   `https://chromewebstore.google.com/detail/feedhacker/kccajfoghkplakndamlohpepopdpelkb` and read
   its **Version** field. That is what users are running. Update `RELEASES.md` if it has moved.

   > **This step used to say "read the newest publish email", and that is wrong.** This item
   > publishes **silently**: neither 0.4.6 nor 0.5.0 ever produced an "Item successfully
   > published" email, yet 0.5.0 is live. On 2026-09-15 the old wording would have concluded
   > 0.4.9 was live and 0.5.0 was stuck in review, and **refused a ship that was perfectly safe**.
   > An absent email is evidence of **nothing**, in either direction. The email is still a useful
   > *positive* signal when it arrives (`from:chromewebstore-noreply@google.com`) — just never
   > treat its absence as a verdict, and never cite a timestamp you have not read.
4. **Check the submission slot.** The store accepts **one pending version at a time**. It is OPEN
   when the newest email is a published/rejected decision for the latest submitted version;
   **BLOCKED** if a version was uploaded and has no decision yet (a new upload then fails
   `ITEM_NOT_UPDATABLE`). GitHub releases are never blocked — only the store upload.
5. **Report and ask.** Summarize what is done and planned for the next release, then ask:
   **ship now, or keep developing?** Never release without an explicit "ship"/"push".

## 4. Close-out checklist (run before ending every session)

The next session starts from what you leave here. Leaving it stale is the whole failure mode.

1. **Update §1 Open items** — delete what closed, add what is newly outstanding, and say who
   each item is waiting on. This is the highest-value thing you will write.
2. **Update §2 Current state** — version, what is confirmed live, dev cycle.
3. **Add a §5 Session log entry** — newest first: the date, what shipped or merged, what was
   diagnosed, and anything a future session would otherwise have to rediscover.
4. **Record outcomes, never predictions** (§44/§45). If you said a workflow or an external
   service would do something, **read its log and write what it actually did.** This has been
   got wrong four times; each needed its own correction PR.
5. **Fold durable lessons outward** — a bug class into `best_practices.md`, a bug into
   `KNOWN_ISSUES.md`, a shipped version into `RELEASES.md`. This file holds *state*, not lessons.
6. **Leave the tree clean** — everything merged to `main` with green CI, or explicitly noted here
   as unfinished with the branch name.

## 5. Session log

Newest first. One entry per session; keep entries short and factual.

### 2026-09-17 (decision) — solo is permanently retired

- **The maintainer's call, in their words: *“ok so solo is permanently retired.”*** Asked first
  whether it could be brought back and made to work, then more sharply whether it was worth
  bringing back at all. The second question is the one that settled it.
- **Two steps, deliberately kept distinct in every record.** Solo was **removed** in 0.8.0
  because LinkedIn's redesign made it actively harmful (it sat before the scorer, so the model
  never ran and the log froze). It was **retired** today as a product decision, on evidence. A
  future reader needs both: §63 exists so nobody mistakes a forced removal for a verdict, and
  §69 now exists so nobody mistakes this verdict for a mere deferral.
- **What the evidence actually was.** (1) Every “hiding everything” report ever received traced
  to solo, none to the AI — and that misdirection cost three rounds of work on the model.
  (2) FH-059's remaining return-condition had **already been shipped in 0.5.0 and already
  failed**; it had been written into the roadmap as future work without anyone checking that
  history had run the experiment. That is the new **§69**. (3) Solo's *correct* output routinely
  looked broken — on the 63-post capture, solo = Hiring matched exactly one post.
- **Nothing replaces it, and that is also the maintainer's call:** *“I don't think options would
  be needed. there is the log in the advanced settings already.”* Verified rather than assumed —
  the AI-slop decision log lists each post with its probability, the tells that fired and the
  matched phrases, and exports as JSON. My earlier suggestion of a new Options panel was
  redundant and I dropped it.
- **One small thing found while checking that, recorded and NOT actioned:** `history[day].byId`
  already stores per-filter hide counts and the Insights panel renders only `total`, so the
  deterministic filters have counts in storage that nothing displays. Noted in §1 item 9 as a
  gap, not a task.
- **Records updated:** `README.md` (user-facing — “we intend to bring it back” → retired, with
  both reasons), KNOWN_ISSUES FH-057, SESSION-STATE §1 item 9 (now the settled decision) and
  item 1 (the store screenshots now advertise a feature that is gone for good — a stronger
  reason to regenerate, still not a blocker), best_practices §69.

### 2026-09-17 (ship) — 0.9.0 released, and FH-055 finally proved itself

- **Shipped on “ship it”.** Release **run #28** from `main` @ `f56d393` (`publish: true`,
  `store: true`, `cancel_pending: true`), conclusion success. Recorded from the job logs, not
  from what was expected: `build` ✅, `webstore` ✅, `release` ✅, `msi` ❌ (`WIX7015` OSMF EULA
  again, `continue-on-error`). GitHub Release **published 22:09:24 UTC**, `draft: false`, four
  zips, no MSI. Store upload `Publish successful` at **22:09:02 UTC** — submitted for review,
  **not** approved.
- **FH-055 is closed, and this run is the proof.** The `webstore` log shows
  `CWS_PUBLISHER_ID: 099a34bd-2c03-4251-a3e0-a4e39f521dad` — a bare publisher UUID, not the
  `project-<uuid>` Cloud-project id that drew an identical 403 on runs #24, #25 and #27. The
  cancel call now authenticates and answers truthfully: **HTTP 400 `FAILED_PRECONDITION` /
  `NOT_CANCELLABLE`**, *“…does not have an active submission that can be cancelled.”* Reporting
  half proven on #27, credential half proven here. **Read from the job log, as
  `CLAUDE.md` says** — the proxy still 403s the Actions API, and a variable still prints in
  plain text while secrets show as `***`.
- **An unexpected bonus: the cancel call is now a second source on the submission slot.** The
  slot was called OPEN before dispatch by reading the store listing; the API independently said
  the same. That is the first time slot state has had two independent confirmations — worth
  reaching for next time, because it costs nothing.
- **The flip side: the call is armed.** It really would withdraw a version in review now, which
  is exactly what `cancel_pending: false` is for. §1 item 11 says so where the next session will
  look.
- **0.9.0's contents**, all shipped today: FH-060 (the 1.5s self-sustaining re-judge loop that
  poisoned the model), the one-time recovery migration that repairs it without asking the user,
  FH-061 (group rows fold by reason) and FH-062 (a slop control on shown posts). Rules §64–§68
  added along the way.

### 2026-09-17 (close, later) — FH-062: the model can finally be told it MISSED one

- **The last of the maintainer's four UX requests is in.** Every FeedHacker control lived inside
  a stub, and a stub exists only once a post is hidden — so *"wrong to hide this"* (label 0) was
  clickable and *"you missed this one"* (label 1) had nowhere to live. Both labels had always
  existed in `onSlopVerdict(id, label, feats)`, which is why the gap read as a UI nicety.
- **It was not a nicety, and that is the lesson (§68).** The model *also* auto-tunes toward
  hiding ~`slopTargetFrac` of what it reviews, so the only signal a user could send pushed the
  threshold the same way the autonomous loop already leaned. A conscientious user correcting
  false positives was walking their own model toward catching **less** — while FH-060's flood
  was damping the strongest tells at the same time. An unreachable label is a thumb on the
  scale, and it presses hardest for the users who engage most.
- **The subtle part was not the button, it was the evidence.** `collapse()` stashed the feature
  vector only when hiding, and `scoreSloppy` returned early for a non-slop post, so a shown post
  had nothing to learn from. It is now stashed for every judged post (gated on `enoughToJudge`,
  so a fragment is never training data in either direction) — and because the node dies on every
  LinkedIn re-render, `remember()` carries it in the **verdict ledger** and the ledger-hit path
  restores it silently. Recomputing it instead would have been FH-049's flood again.
- **Two tiers, all mutation-tested:** 9 integration cases (including the control surviving five
  re-renders with zero new decisions and zero new observations) and a system case that clicks it
  in real Chromium and reads `feedhacker:sloptrain` back holding exactly one `label: 1` example —
  the whole loop through the delegated click, the verdict queue and the debounced write. Triad:
  **303 unit+integration, 34 system**.
- **One thing deliberately NOT changed:** the click guard is still reactive (`onInteract()` fires
  from inside the click handler). FH-060 removed the 1.5s stub rebuild that made that bite
  constantly, so it may already be fixed in practice — §1 item 10 says to ask before touching it
  rather than guess.

### 2026-09-17 (close) — FH-061 grouping, and the FH-060 repair now ships itself

- **The AI reset is automatic in 0.9.0**, on the maintainer's call: *"make .9 upon install perform
  the reset once during the install so users in the wild don't have to be told to do it."* Right
  call — a code fix ships, stored state does not, and the installs still running a poisoned model
  are disproportionately the ones whose owners never read release notes. The service worker now
  clears the AI-slop learning once on update, keyed in a `feedhacker:migrations` ledger, with the
  ledger written **inside** the clear's own callback (a worker torn down mid-migration re-runs it
  rather than recording a repair that never happened) and **banked as done on a fresh install**
  (otherwise a user's first update destroys a model they trained themselves). The key lists moved
  to `filters.ts` so the options button and the migration clear one definition — two copies is how
  FH-054 happened. New rule **§67**.
- **FH-061: a group row now stands for one reason.** `groupRuns` folded on adjacency alone, so a
  mixed run became *"3 posts hidden · AI Slop ×1, Promoted Post ×2"* — and the row's splat could
  only train the slop member, while expanding gave the others no control either (a deterministic
  hide carries no feature vector). `reasonKey(el)` ends the run when the primary reason changes.
- **Three contracts changed on purpose, each re-pinned rather than relaxed.** "An upgrade writes
  nothing at all" became "an upgrade clears the AI-slop learning once and nothing else, ever",
  with the user's own setup asserted item by item. "No `feedhacker:` key survives a factory reset"
  gained ONE exemption, `feedhacker:migrations`, listed by name with its reason at both the unit
  and system tiers — clearing it would re-arm a destructive migration. And the `install-upgrade`
  mock grew a real backing store, because run-once cannot be tested against storage that always
  reports empty.
- **Everything new is mutation-tested at the source** (§61): the grouping split (integration +
  system), the ledger write, the fresh-install banking, and the worker's `importScripts` — that
  last one matters because the migration self-skips when the module is missing, so a failed
  import would leave every poisoned install unrepaired and say nothing. Full triad green: **294
  unit+integration, 33 system**.
- **Still open and specified: §1 item 10(c)** — no controls at all on a shown post, so the model
  can never be told it missed one. That is the remaining half of the maintainer's request and the
  one with the most leverage on "it isn't catching all the slop".

### 2026-09-17 (later) — FH-060: the feed was being re-judged every 1.5 seconds, by itself

- **The maintainer's export was the first valid one, and it found a SECOND cause.** `version: 0.8.0`,
  all 300 entries that day, calibration fresh with `labels: 0` — the reset had landed. It read **300
  decisions over 5 distinct posts**, median gap **1548ms and identical to the millisecond across the
  top three** (one clock drove them), and **189 observations → 12 distinct feature vectors** (×14–29).
  FH-049's re-render fix was real and still holding; this was something else on top of it.
- **The clue that saved hours: the duplicates were byte-identical.** Identical feature vectors mean
  the scored *text* never changed, so post identity was not slipping — the scorer was simply being
  called again. That moved the search to the scheduler and straight onto `content.ts:717`.
- **Root cause, a closed loop inside one tab.** Hide a post → bump the author's hide tally →
  `saveAuthorsSoon()` writes it after 1500ms → `storage.onChanged` delivers that write **back to the
  tab that made it** → the handler called `reapply()` → `reset(doc, true)` drops every model-derived
  verdict → rescan judges everything → hides → bumps tallies → forever. No scrolling, no clicks. The
  FH-049 ledger could not help: it was emptied every turn. `recompute()`'s careful `onSlopObserve`
  muting missed it because this path runs through `scanNow()`.
- **The harm was the learning, not the log.** Auto-calibration read `broetry` as firing on **76.75%**
  of the "population", damped it **1.400 → 0.792** and `bullets` **0.900 → 0.516**, and pushed the
  threshold **0.500 → 0.570**. The model had taught itself to catch less — which is exactly what the
  maintainer reported. A code fix cannot undo it; §1 item 3 is the one click that can.
- **Fix:** compare the mute/allow **rules** (`Authors.rulesKey`/`sameRules`), not the raw store, so a
  tally-only write is inert while a real mute still re-applies at once. Three tiers, both
  mutation-tested at the source: integration reports "reset called 20x", and the **system** case
  reproduces it in real Chromium — **12 decisions over 3 posts in a 7-second dwell with no input**,
  3 after the fix. Rules **§64** (never react to your own storage write; compare the projection you
  consume), **§65** (read extension storage from a page, not an idle MV3 worker — that hangs) and
  **§66** (re-measure the symptom, not the cause you fixed) added.
- **Two harness faults found on the way**, both now fixed and both of the "looks like a product bug"
  kind: `ctx.serviceWorkers()[0]` can return a worker with `chrome` unbound (new `extensionWorker()`
  resolves the extension's own), and the fixture launcher sends no charset, so the first non-ASCII
  fixture was decoded as windows-1252 and the scorer saw mojibake.
- **Also answered, from the code, and logged as §1 item 10:** grouping folds by adjacency rather than
  by reason (so AI slop and Promoted share a row), the group splat is gated on slop members, and
  there are **no controls at all on a shown post** — so the model can never be told it missed one.

### 2026-09-17 — 0.8.0 is LIVE on the store; the FH-049 measurement is unblocked and specified

- **0.8.0 is published.** Read from the **store listing** (`?hl=en-NZ`): “Version 0.8.0, Updated
  17 September 2026”. Uploaded 20:01:33 UTC the previous day, so review cleared overnight — longer
  than 0.6.0 (~14 min) or 0.7.0 (~30 min), and it was still showing 0.7.0 at the 43-minute mark.
  **Slow is not rejected**, and the previous release's wait does not bound the next one's. §1 item 8
  closed, `RELEASES.md` 0.8.0 store column set **Live**, submission slot **OPEN**.
- **The maintainer has 0.8.0 installed and reports the feed “appears to be working” without solo** —
  consistent with v0.7.0 behaving with solo off. That is an encouraging observation, not a
  measurement, and it is not yet evidence about FH-049 either way.
- **§1 items 2 and 3 rewritten as one concrete procedure**, because they are the same single click:
  Options → Error log → **Reset AI-slop learning** *first* (it clears the log as well, so doing it
  after a browse throws away the very data wanted), then browse ~50–100 posts, then Options →
  AI-slop decision log → **Export log (JSON)**. An export counts only if `version` reads `0.8.0`,
  the newest `log[].iso` is that day's date, and **decisions ≈ distinct posts** — 300 decisions over
  13 posts was the FH-049 signature.
- **Item 3 is no longer harmless.** The flood-era `threshold: 0.6569` was inert only because solo
  kept the scorer from running. On 0.8.0 the scorer runs every scan, so that polluted calibration is
  what is deciding the maintainer's feed until it is reset.

### 2026-09-16 (close) — reframed solo's removal: forced by LinkedIn, not a product verdict

- **The maintainer corrected the record, and the correction matters more than the edit.** Every
  document described solo's removal as a judgement on the feature — `best_practices.md` §57 even
  asserted "the design is the defect". That is not what happened. The trigger was **external**:
  LinkedIn rebuilt its feed during a window in which **`src/` went 51 days without a single
  commit**, removing the hooks post identity and furniture detection relied on (FH-049/052/053).
  The reports followed the platform change, not a design choice.
- **What solo actually did wrong was structural, not conceptual.** It decided what to hide
  **before the scorer**, so while it was on the AI-slop model never ran and `sloplog` never
  advanced. On a feed already misreading itself that both amplified the symptom and concealed the
  cause — three exports frozen at 2026-09-02, the FH-049 measurement impossible for 13 days.
  Removing it restored the instruments. **That** is the justification, and it stands on its own.
- *(Superseded 2026-09-17: solo was RETIRED permanently — see the 2026-09-17 decision entry above and §1 item 9.
  This entry is left as the accurate record of what this session concluded at the time.)*
- **Solo is deferred, not rejected**, and the records now say so in every place a future session
  would look: CHANGELOG (user-facing), README, KNOWN_ISSUES FH-057, RELEASES, SESSION-STATE §1
  item 9, and roadmap **FH-059** for its rebuilt return. Two conditions on coming back: it must
  never short-circuit the scorer, and an empty result must explain itself with a one-click exit.
- **§57 amended and §63 added.** §57 was overstated; §63 is the rule that would have caught it —
  before deleting a feature that "causes" a spike, check what changed *underneath* it, and use
  `git log` rather than memory. Removing something that **blinds your diagnostics** is justified
  on its own; removing something merely **correlated** with a spike is not.

### 2026-09-16 (later) — guarded the armed cancel, and killed a flaky system test

- **Documented the environment boundaries in `CLAUDE.md`** (new *What a session can and cannot
  reach* standing rule) so the "can't you just read the Actions variable?" round-trip does not
  happen again. The honest shape: **the workflow uses `vars`/`secrets` fine**; a *session* cannot
  read them via the API (the agent proxy 403s Actions paths — tested, both endpoints); but a
  **variable's value prints in plain text in a job log** while secrets show as `***`, which is how
  the wrong id was found in the first place. Also recorded the API status lag, the `WebFetch`
  15-minute per-URL cache, no `gh` CLI, and `npm ci` on a fresh container.
- **Guarded the armed cancel** with a new `cancel_pending` input (defaults **true**, so nothing
  about the normal release changes). It exists for one case: re-uploading the *same* version that
  is already in review, which the idempotent re-cut makes easy to hit by accident. The gate is
  written `github.event_name != 'workflow_dispatch' || inputs.cancel_pending` because a **tag push
  carries no inputs** — the §59 trap, caught here by a test rather than by a release.
- **FH-058: the popup system tests raced the first paint.** CI went red on a records-only PR whose
  diff was three markdown files. Not a flake to re-run — a real race: the `.ms` buttons are static
  markup, so `waitForSelector` resolves before the async settings callback paints `m-on`. Fixed by
  waiting on `#enabled` (independent of the assertion, so it synchronises without muzzling).
- **Two lessons that cost a wasted mutation run each.** §61: the first mutation test patched
  `build/popup.js`, then the build regenerated it from `src/` and erased the mutation — five
  green tests proving nothing. §62: wait for the *state* you assert, not the markup that will hold
  it, and never wait on the very class under assertion.

### 2026-09-16 (late) — the publisher id was fixed, which armed a blind destructive call

- **The maintainer replaced `CWS_PUBLISHER_ID` in the repo Action variable**, closing the
  credential half of FH-055 after three identical 403s (runs #24, #25, #27).
- **It is UNVERIFIED and cannot be verified from here.** Worth stating precisely, because the
  first version of this note got the reason wrong: the REST endpoint *does* exist and the session
  *does* have a `GITHUB_TOKEN` — but this environment's agent proxy refuses Actions paths outright
  (`403 “Access to this GitHub Actions path is not permitted through this proxy”`, tested on both
  `actions/variables` and `actions/variables/CWS_PUBLISHER_ID`), and the GitHub MCP server exposes
  no variables tool. That is a deliberate boundary — an agent should not read or write CI
  variables — and not a gap to route around. The `webstore` job echoes the value in plain text, so
  the **next release log** is the first place to confirm it: expect a 200/204 or 400/404 from the
  cancel step instead of a 403.
- **The important consequence is not the fix, it is what the fix switches on.** `cancelSubmission`
  is called **blind and unconditionally** before every store upload. While it was refused it was a
  guaranteed no-op, and the pipeline had quietly come to depend on that. It can now actually
  cancel — and **0.8.0 is in review right now**. A store release dispatched before 0.8.0 publishes
  would withdraw it and return it to draft, where the same action previously failed safely with
  `ITEM_NOT_UPDATABLE`. **Refused → silently undone.** The idempotent same-version re-dispatch used
  earlier today is no longer harmless.
- Recorded as **§60**: a broken destructive call is not a safe one; repairing its credentials arms
  every blind invocation at once, against whatever state happens to exist at that moment.

### 2026-09-16 (store) — 0.8.0 uploaded to the Chrome Web Store

- **The hold lasted ~18 minutes.** On *“push 8.0 to the store”*, run #27 dispatched with
  `publish: true` **and `store: true`**. `webstore` ✅: `Uploading feedhacker-0.8.0-store.zip… /
  Publishing… / Publish successful` at **20:01:33 UTC** — **submitted for Google review, NOT
  approved**. `release` ✅ re-cut the same tag idempotently; `msi` ❌ on `WIX7015` as always.
- **Slot checked the right way first.** Read the **store listing** (“Version 0.7.0, Updated
  September 15, 2026”), not the inbox — the 0.5.0/0.6.0 lesson.
- **FH-055 recurred a third time, and our half of it is now proven fixed in production.** Same 403
  `PERMISSION_DENIED` on the same `project-46303a79-…` id, so it is reproducible. But this was the
  first release to run the **corrected** message, and it printed the refusal honestly instead of
  calling it “normal when nothing is in review”. The reporting fix is no longer only unit-tested.
  The wrong id remains the maintainer's to replace — and it matters more now, because the slot is
  **blocked** while 0.8.0 is in review and `cancelSubmission` is the thing that would free it.
- **The stale screenshots were raised before the upload and deliberately deferred** — *“we'll make
  changes to the store carousel if we need to. this is part of our text to resolve the latest
  issues.”* Recorded in §1 item 1 as accepted, so no future session re-raises it as a blocker.
  Listing assets are an independent channel and can be fixed without a new package.
- **The new `store` input earned itself in one day**: `store: false` for the GitHub-only test ship,
  then `store: true` for the real upload, with no workflow edit between them.

### 2026-09-16 (release) — 0.8.0 shipped to GitHub, store deliberately held

- **Asked to ship 0.8.0 to GitHub only. The workflow could not do that.** The `webstore` job's
  `if:` was character-for-character the `release` job's, so `publish: true` — the way every
  previous release was dispatched — would have uploaded to the store and, with
  `CWS_AUTO_PUBLISH=true`, auto-published to **every existing install**. Found by reading
  `release.yml` before dispatching rather than after.
- **Fixed with a second input, `store`, defaulting to `true`** so every existing habit publishes
  exactly where it always did. A tag push has no inputs at all and keeps both destinations
  unconditionally. PR #91, merged `48bc6c8`.
- **The gates are tested by EVALUATING them, not grepping them** (`test/unit/release-gates.test.js`,
  6 cases). A gate is a boolean expression and the shape that bites is the one with no inputs: on a
  tag push `inputs.store` is `undefined`, not `true`, so a gate written without its tag branch
  reads perfectly and silently stops shipping to users. The translator **rejects** any construct it
  does not model rather than guessing. Mutation-checked three ways (re-couple the gates, drop the
  tag branch, default the input off); each failed only the cases it should. **§59.**
- **Release run #26 — the hold is verified, not assumed.** `webstore` conclusion reads **`skipped`**
  (created and completed 19:42:33 UTC, zero steps), so nothing was uploaded and **the cancel step
  never executed** — FH-055 was not exercised this release. `release` ✅: tag `v0.8.0` pushed, GitHub
  Release published **19:43:09 UTC**, `draft: false`, four zips, no MSI.
- **`msi` failed as always, and the shorthand in the records turns out to be exactly right.** The
  real error is `WIX7015: You must accept the Open Source Maintenance Fee (OSMF) EULA to use WiX
  Toolset v7`, raised at `wix build` — WiX v7.0.0 itself installs fine in 8s. `continue-on-error`,
  blocks nothing.
- **Two channels now diverge for the first time:** GitHub has 0.8.0, the store has 0.7.0. That is
  the point — the filtering changes get tried on a real feed before they reach every install
  automatically. Making the store upload is now **§1 item 8**, and it will not happen on its own.
- **Corrected a stale row while here:** §2 "Confirmed live on the store" still read 0.5.0 even
  though 0.6.0 and 0.7.0 had each been confirmed live. Now 0.7.0.

### 2026-09-16 (docs sweep) — the solo removal had missed eleven files

- **Asked "did you change all the documentation, help text, and settings page?" — the answer was
  no.** The code removal (FH-057) updated the popup help and the options Actions card, but eleven
  other files still described Solo as a live feature. Two of them reach users directly:
  **`manifest.json`'s description**, which ships in the package *and* is the Chrome Web Store
  summary, and **`store/listing.md`**, the listing copy itself. Also `welcome.html` (the
  post-install page), `README.md`, `INSTALL.md`, `docs/index.html`, both privacy-policy copies,
  `CLAUDE.md`, `types/feedhacker.d.ts` and `scripts/build.mjs`. All corrected.
- **History was annotated rather than rewritten.** `roadmap.json` FH-010 and `the_wall.md` record
  that the Mute/Solo mixer shipped in 0.1.0 — true, and left standing, with a `supersededBy`
  pointer to the new FH-057 entry. Deleting the row would have made the ledger lie about the past.
- **The screenshots are still wrong, and that is now open item 1.** Three images show the removed
  control. There is no screenshot generator in the repo, so this is real work; it blocks the
  **store** submission of 0.8.0, not the GitHub release.
- **Lesson worth keeping (§58):** removing a feature is not done when the code is gone. The copy
  that describes it lives in packaging metadata, store listings, onboarding pages and images —
  surfaces no test touches and no compiler checks.

### 2026-09-16 — solo mode removed (FH-057)

- **The maintainer proposed removing solo entirely; the evidence supported it, so it is gone.**
  Solo showed *only* the soloed kinds and hid the rest, so one toggle could empty a feed and the
  result was indistinguishable from a broken filter. Counting the ledger rather than recalling it:
  **every "FeedHacker is hiding everything" report this project ever received traced to solo, and
  none to the AI** — FH-051, then three more in a single day.
- **The hidden cost was larger than the confusion.** Solo short-circuited *before* the scorer, so
  while it was on the AI-slop model never ran and its decision log never advanced. That is why
  three consecutive exports held 13-day-old data and why the FH-049 measurement had been
  impossible. **Removing solo unblocks open item 1.**
- **0.5.0 had already tried mitigation** — naming the mode on the stub, a **Show everything** exit
  on every row and group. Three further reports followed. That is what settled it, and it is the
  general lesson: mitigation gets one honest attempt, then the design is the defect (**§57**).
- **Removing it also deleted a bug class.** FH-056 existed *only* because solo and author-mute
  could disagree about precedence; it cannot recur, and is marked superseded.
- **The migration was the risky half, not the deletion.** Installs still carry `solo<Key>: true`
  in sync. A stale key is now **deleted** from live settings on every load (`applyFixed`) and
  **evicted from sync storage once** (`content.ts`), so it can neither act nor keep syncing
  between devices. 7 cases in `test/integration/solo-removed.test.js` pin that, including a guard
  against a half-removal, and the options system test now asserts the legacy key is *gone from
  sync*, not merely false.
- **The popup is regrouped**: **AI slop** (own on/off + sensitivity) at the top, since it is a
  learned model rather than a fixed rule; the plain kind toggles below under **Also hide**.

### 2026-09-15 (21:57 export) — the 403 message fixed, and the third stale log explained

- **Fixed the FH-055 reporting half.** The cancel step lumped every non-2xx into *"This is normal
  when nothing is in review"* — false for the 403 it actually got, twice. Now: 200/204 cancelled,
  400/404 routine empty queue (no warning), **401/403 a REFUSAL** with a `::warning` that echoes
  the offending `CWS_PUBLISHER_ID` and names where the right one lives, anything else UNKNOWN.
  Mutation-tested: 5 of 6 new cases fail on the old message. **The id itself is still wrong** —
  that half stays open and is the maintainer's.
- **"Still blocking most posts" is solo mode, for the fourth time — and now provably not the AI.**
  The capture: **97 posts, 93 hidden, every one of them `Solo mode: showing only Hiring posts`.**
  Zero author-mutes, zero slop hides. **FH-056 is visibly working**: 2 hiring posts survived,
  including Richard King's — the muted author whose hiring post used to be discarded — plus the 2
  furniture modules FH-053 leaves alone.
- **The decision log was stale for the third time, and we finally know why.** Entries all date
  from **2026-09-02**, with the original pre-fix signature (300 decisions, 13 distinct posts, one
  ×42). `sloplog.push()` keeps the newest 300, so a frozen log means **no decision has been logged
  in 13 days** — because solo short-circuits *before* the scorer runs. **The FH-049 measurement is
  impossible while solo is on.** Item 1 now spells out the order: reset → solo OFF → browse →
  export, and says to check `log[].iso` before sending.
- **A flood-era calibration is still live** in that install: `threshold 0.6569`, calibrated
  2026-09-08 from 164 observations gathered during the duplicate flood, with an empty weights
  export alongside — the FH-054 signature. Inert under solo, but it will drive behaviour the
  moment solo is off. New open item; one Reset AI click clears it on 0.7.0.

### 2026-09-15 (20:18) — 0.7.0 is LIVE, and a correction to this morning's own correction

- **0.7.0 published 19:54:24 UTC**, ~30 minutes after the 19:24:28 upload. Confirmed two ways:
  the listing reads *"Version 0.7.0, Updated September 15, 2026"*, and Google's publish email for
  Version 0.7.0 is timestamped 19:54:24 UTC. Open item closed. **FH-056 is now in users' hands.**
- **I got the "silent publish" claim wrong, and it went into the records.** Earlier today this
  file and `RELEASES.md` asserted that 0.4.6, 0.5.0 **and** 0.6.0 all published with no email —
  "three for three", promoted from correction to established property.
  **0.6.0 did email**, at 18:24:40 UTC. The search that "proved" otherwise was
  `from:chromewebstore-noreply@google.com newer_than:2d`, and **that message is in Trash, which
  Gmail excludes from search by default**. The 8-thread historical search that cleared 0.4.6 and
  0.5.0 *did* use `in:anywhere`, so those two stand — but the generalisation did not.
  Both records are corrected in place rather than quietly edited.
- **Two consequences worth carrying forward.** (1) 0.6.0's real turnaround was **~14 minutes**,
  not the "within ~54 minutes" upper bound first recorded — 54 minutes was simply when it was
  looked at, which is the same shape of error as the 0.5.0 row. (2) The durable rule is narrower
  and more useful than "this item publishes silently": **a negative search result is only
  evidence if the search covered everywhere it could be.** `in:anywhere`, or it proves nothing.
  Codified as §56.
- **Reading the listing was still the right call** and is what caught this: it never depended on
  which folder an email landed in.

### 2026-09-15 (ship 0.7.0) — shipped, and FH-055 proved deterministic

- **Shipped 0.7.0** on the maintainer's explicit "ship 0.7.0", into a slot confirmed open by the
  listing read that also closed the 0.6.0 item. Release run #25: `build` ✅, `webstore` ✅,
  `release` ✅, best-effort `msi` ❌ on the WiX gate as every release. **GitHub Release published
  19:24:58 UTC**; store log `Uploading feedhacker-0.7.0-store.zip… / Publishing… / Publish
  successful` at **19:24:28 UTC** — submitted for review, not approved. New open item tracks it.
- **FH-055 reproduced exactly.** Same `CWS_PUBLISHER_ID` (`project-46303a79-fd20-4ed8-859`), same
  **403 `PERMISSION_DENIED`** on the same resource path, same misleading *"This is normal when
  nothing is in review"*. Two-for-two means it is deterministic, not a transient, and the id
  really is the wrong kind of identifier. It cost nothing again — the slot was free.
  **The message half is ours and is now a proven-recurring false green**: only 404/`NOT_FOUND`
  should read "nothing in review"; 401/403 should warn. That fix is a candidate for 0.8.0.

### 2026-09-15 (late) — 0.6.0 confirmed live, 0.7.0 cut

- **0.6.0 is LIVE.** Confirmed 19:04 UTC by reading the store listing: *"Version 0.6.0, Updated
  September 15, 2026"*. Submitted 18:10:48 UTC, so it published inside ~54 minutes — the exact
  instant is not knowable from the listing and is deliberately not invented. Open item closed.
- **No publish email arrived, again.** `from:chromewebstore-noreply@google.com newer_than:2d`
  returns nothing. That is **three consecutive silent publishes** — 0.4.6, 0.5.0, 0.6.0 — so the
  corrected checklist step 3 is no longer a correction to a one-off mistake; it is a property of
  this store item. §2's "how to check" row now says so.
- **Shipped 0.7.0 on the maintainer's explicit "ship 0.7.0"**, into a slot confirmed open by that
  same listing read. It carries **FH-056** — the author mute that outranked solo and discarded
  the one genuine hiring post in a 63-post feed.
- **Method note:** the ship pre-flight and the scheduled 0.6.0 check-in turned out to be the same
  action. Reading the listing answered both "did 0.6.0 publish?" and "is the slot free?" in one
  call. Worth remembering: for this project those two questions have one source.

### 2026-09-15 (evening) — FH-056: the filter found the post, the other setting threw it away

- **The maintainer reported "still have same issues with 6.0" with a third live capture.** It
  carries `data-feedhacker-furniture`, an attribute that exists **only in 0.6.0**, so the build
  was confirmed from the artifact rather than assumed (§49) — they are on 0.6.0, and FH-053 is
  working (the jobs module was recognised and left visible).
- **My previous diagnosis was incomplete, and that cost a round.** I had said "solo mode, working
  as designed, turn it off." Reading all 63 posts instead of the counter showed the real fault:
  solo was Hiring, exactly **one** post qualified — *"Disney is hiring! Hundreds and hundreds of
  posted roles"* — and it was hidden by **`Muted author: Richard King`**, because `consider()`
  checks the author lists *before* it computes solo, and the mute branch returns.
  So the feed was empty not because nothing matched, but because the one match was discarded
  after being correctly identified. **FH-056**, fixed: in solo mode the soloed kinds are the
  whitelist, so a muted author's *matching* post now shows; their other posts are still hidden by
  solo, so the mute loses nothing. Codified as **§55** — evaluate the narrowing filter before the
  excluding one, or the excluding one silently voids it.
  Validated by replaying the real capture with the maintainer's own 11 muted authors: **0 visible
  posts → 1**.
- **`isHiring()` needed no change and got none.** The maintainer's spec — *only explicit hiring
  for real open roles; not new employment, new role, new job, anniversaries, commentary* — is
  what it already does. It found the one real ad in 63 and correctly rejected the same author's
  three other posts, plus "I'm starting a new position at Cisco" (that is the `job` category) and
  "completed 3 years" (`anniversary`).
- **"Don't block a post just for mentioning AI" — already true, and now measured.** 28 of the 63
  posts mention AI, 35 do not; mean slop probability **0.168 for both**, 0 of 63 hidden as slop.
  No topic features exist in the scorer. Recorded as open item 2 so it is not re-litigated, with
  the caveat that a self-tuned model could differ and that the decision log is the way to tell.

### 2026-09-15 (ship) — v0.6.0 cut, and the cancel step finally failed out loud

- **Shipped 0.6.0 on the maintainer's explicit "ship it".** Release run #24: `build` ✅,
  `webstore` ✅, `release` ✅, best-effort `msi` ❌ on the WiX gate as every release.
  **GitHub Release published 18:11:27 UTC** with all four prebuilt zips. Store upload log:
  `Uploading feedhacker-0.6.0-store.zip… / Publishing… / Publish successful` at **18:10:48 UTC** —
  **submitted for review, not approved.** Open item 5 tracks confirming it.
- **The pre-flight slot check caught a false record, and the checklist that produced it.**
  `RELEASES.md` and this file both claimed 0.5.0 was confirmed live by a Google email at
  "2026-09-08 20:55:30 UTC". **No such email exists** — the newest publish email this account has
  ever received is Version 0.4.9. The webstore job for 0.5.0 finished at 20:39:05 UTC and the
  rest was inferred, then written down as a quotation. Fifth instance of §44/§45.
  0.5.0 **is** live — the **store listing** says so ("Version 0.5.0, Updated September 8, 2026").
  **The deeper problem was the procedure**: §3 step 3 of this file said to read the newest
  publish email. Followed literally today, it would have concluded 0.4.9 was live and 0.5.0 was
  stuck in review, and **refused to ship**. This item publishes silently — 0.4.6 and 0.5.0 both
  did — so §2 now carries a standing **"How to check what is live"** row: read the listing.
- **FH-055 — the store cancel step reached Google for the first time and was refused.**
  `CWS_PUBLISHER_ID` is no longer empty (it resolved to `project-46303a79-fd20-4ed8-859`), which
  answers the question five releases of warnings could not. But `cancelSubmission` returned
  **403 `PERMISSION_DENIED`**, and the value has the shape of a **GCP project id**, not a Chrome
  Web Store **publisher** id. Separately, and this half is ours: the step prints *"HTTP 403 …
  This is normal when nothing is in review"*, which is **false** — a refusal is not an empty
  queue. That is the FH-048 false-green class surviving in the non-200 branch. Neither blocked
  the release; the slot was free, as it has been for every upload since 0.4.7.
- **Method note worth keeping:** every claim in this entry came from the job log, the store
  listing or the Releases API. The one thing not yet verifiable — whether 0.6.0 publishes — is
  recorded as *submitted*, not as *live*.

### 2026-09-15 (later) — the maintainer's reset spec, and the button that was lying

Same session as the entry below; kept separate because it came from an explicit behaviour spec
rather than from the captures.

- **The maintainer specified what each reset must do.** Checked each clause against the code
  rather than assuming:

  | Clause | Was it already true? |
  |---|---|
  | Fresh install: AI-slop on, no other mute, nothing soloed | **Yes** — `buildDefaults()`, already tested |
  | Upgrade leaves existing settings alone | **Yes** — `onInstalled` writes nothing, already tested |
  | Factory reset: everything gone, back to AI-only, no muted users | **Yes** — `LOCAL_KEYS` + `sync.clear()` + `buildDefaults()` |
  | Reset AI: wipe learned data, restore the shipped algorithm, **keep** mute/solo | **NO — this was broken** |

  So **open item 5 closes as confirmed, not changed** — three of the four clauses were already
  the shipped behaviour. They now have tests pinning the exact shape, which they did not before.
- **Fixed FH-054 — "Reset AI-slop learning" did not reset the AI.** It removed `WEIGHTS_KEY`
  alone. The training buffer, the observation pool, the calibration record and the tuned
  `slopThreshold` in sync all survived, so auto-calibration rebuilt the same model from the
  leftovers within a scan or two. The code's own comment on "clear log" already claimed this
  button would "wipe the model itself".
  **This one bites item 1**: that procedure says *reset, then export the decision log*. Any
  export taken after a pre-0.6.0 reset measured a model that had quietly restored itself, so it
  is not evidence either way. Item 1 now says to run it on 0.6.0 or later.
- **Fixed FH-053 — LinkedIn's feed modules were being treated as posts.** Closes open item 6.
  The guard needs **both** a known module heading and the absence of a per-post overflow
  control, and fails CLOSED: mistaking a post for a module would silently exempt it from
  filtering, whereas missing a module only keeps today's behaviour. Validated on the live
  captures — 1 of 8 and 2 of 42 containers flagged, exactly the modules present, no false
  positives on the other 40.
- **Item 4 now ships a release note.** 0.6.0's CHANGELOG (which `store/README.md` makes the
  source of the store's release notes) tells hand-installed Windows users on ≤0.4.5 to re-run
  `installer\install.bat`, and says plainly that Chrome Web Store users are unaffected. The item
  stays **open** on purpose: the people who need it are the ones **not receiving updates**, so a
  note in a release they never get cannot reach them.
- **Two of my own test bugs, caught by mutation-testing rather than by the suite going green.**
  Worth recording because both would have shipped as false green: (1) a single-marker test
  document makes `postContainerFor()` walk to the document root, so three cases were asserting
  against `<html>` rather than the post — every test file here must keep **two or more** markers
  on the page; (2) a muted-author case used the display name as the mute key when `keyFor()`
  builds `/in/slug`, so it passed without the fix. Both now fail correctly when the fix is
  removed. Codified as **§54**.

### 2026-09-15 — a dead session, two live DOM captures, and a watchdog that had quietly died

- **Context: this session was asked to pick up a sibling session's work.** `Feedhacker LinkedIn
  bug` (`session_01CtWmAPZukKJt83QZNhkNVr`) ran ~9 minutes and **failed with "Prompt is too
  long"**. It never committed or pushed; its branch `claude/loving-cannon-hn62q0` does not exist
  on the remote and its transcript is not readable from another session. **Its diagnosis is
  unrecoverable** — treat a sibling session's findings as lost unless they are on a branch.
- **The maintainer supplied two live home-feed DOM captures** (one without FeedHacker, one with).
  Those are the first real-markup artifacts this project has had, and they settled three things
  that three prior sessions could only theorise about.
- **"The feed is empty" was NOT the filter — again (§51, third time).** The with-FeedHacker
  capture: **42 posts, all 42 hidden** — **23** by `Solo mode: showing only Hiring posts` and
  **19** by muted authors. Only **one** post in the whole capture classifies as hiring, and its
  author (Richard King) is **himself muted**, so solo=Hiring had literally nothing left to show.
  Working as designed; the 0.5.0 exit UX was rendering correctly (3 group rows, each naming solo
  mode and carrying **Show everything**). No code defect.
- **Fixed FH-052 — the "selectors are out of date" alarm had been dead since LinkedIn's
  redesign.** `heartbeatBreak()` only fires on `markers === 0 && content > 0`, and `contentCount()`
  looked for `role="article"` / `data-urn` / `data-id`. LinkedIn's current feed ships **none of
  them**: **zero matches against 8 and 42 visible posts.** So `content` was pinned at 0, the alarm
  could never fire, and the one mechanism that would tell us our marker had gone stale was itself
  broken — silently, with nothing red. Fixed additively by also counting LinkedIn's per-post
  overflow control (`[aria-label^="Open control menu for post"]`, 7/8 and 40/42). Codified as
  **§53**: a watchdog needs a fixture cut from the real product, because its own tests built the
  `role="article"` markup they tested against and so could never exhibit the bug (§49).
- **Post identity: question closed.** Item 1 used to hypothesise that a lopsided decision count
  meant "the activity-URN lookup is not finding LinkedIn's post ids". Confirmed, and it is not
  fixable the obvious way: **LinkedIn no longer puts the URN on the post container at all**
  (42/42 posts fell back to `textHash`). Harvesting it from reshare permalinks was considered and
  **rejected** — a reshare embeds the *original* post's permalink, so it would collide two feed
  entries onto one verdict. Recorded as open item 2.
- **Encouraging, but not proof:** those 42 posts produced **42 distinct keys, zero duplicates**.
  That is one snapshot, not the time series FH-049 was diagnosed from, so item 1 stays open —
  narrowed to "export the decision log".
- **New finding, not fixed:** LinkedIn furniture ("Who's viewed your profile", "Jobs recommended
  for you") still enters the scan as posts. FH-050 gated only the AI-slop path behind a 20-word
  prose minimum; solo and mute have no such gate. Open item 6.
- **PR [#79](https://github.com/newellnarco/Feedhacker/pull/79) is still open from 2026-09-09** and
  its version-bump half is now duplicated by this session's 0.6.0 bump. It can be closed, or
  merged first and this branch rebased. Flagged to the maintainer; not actioned unilaterally.

### 2026-09-08 — "it hides everything", three releases, and the solo-mode red herring

- **Shipped 0.4.7, 0.4.8, 0.4.9 and 0.5.0**, all four on explicit maintainer instruction.
  0.4.7/0.4.8/0.4.9 confirmed published on the store (22:57, 03:24 and 04:25 UTC); 0.5.0
  **published 20:55:30 UTC**, 16 minutes after upload (Release run #23) — so all four versions
  shipped this session are live.
  The store-cancel step ran and warned `CWS_PUBLISHER_ID is empty` for the **fourth** release
  running, so `cancelSubmission` has still never been called; every upload has gone into a free
  slot instead.
- **Fixed FH-043 … FH-051** — see `KNOWN_ISSUES.md`. The arc: Mute keyed on the wrong author →
  the AI-slop splat unreachable on folded runs → the house style guide scored as evidence of AI
  authorship → the hidden share was a quota rather than a judgement → posts re-judged on every
  LinkedIn re-render (300 decisions from 13 posts) → LinkedIn furniture hidden as posts.
- **The last report was not the AI at all.** "Still hiding nearly 100%" was **solo mode**:
  `"Filtered out"` occurs at exactly one place in the source, and that branch returns before the
  scorer is consulted. Two sessions of scoring work had been aimed at a symptom that had nothing
  to do with scoring. Codified as §51 — grep the strings in a screenshot before reading the model.
- **Added** Unmute all authors, Factory reset, and guards that an upgrade preserves settings.
- **All four shipped versions are live on the store** — 0.4.7 (22:57), 0.4.8 (03:24), 0.4.9
  (04:25) and 0.5.0 (20:55 UTC). Review turnaround ran 4h → 2h → 20min → 16min, so every upload
  went into a free slot and the cancel step has never once been needed. The reason it never ran
  was finally pinned down at the end of the session: `CWS_PUBLISHER_ID` was never added to the
  repository at all — Actions → Secrets holds only the three OAuth credentials and Actions →
  Variables only `CWS_AUTO_PUBLISH` — so no amount of tab- or scope-hunting would have found it.
  Three sessions theorised about *where* it was; nobody checked *whether* it was there (§51 again:
  look at the thing before reasoning about it).
- **`best_practices.md` §36–§52** written this session; `TEST_MATRIX.md` gained rows for
  destructive controls, install/upgrade, and solo/mute.
- **Method failures worth not repeating:** a store outcome recorded as fact before reading the
  log (three times, corrected in #62, #70, #71); two rounds of scoring fixes validated against a
  corpus written by the person fixing the bug, which by construction could not exhibit the defect
  (§49); and a user-supplied log accepted without checking its `version` field — it was the same
  pre-fix export twice (§49 again).

## 6. Key facts & gotchas (so a new session doesn't relearn them)

- **Ship only on explicit "ship"/"push."** Otherwise keep developing, commit/merge PRs freely
  (green CI), accumulate under the next version.
- **Release mechanism:** run the **Release** workflow via `workflow_dispatch` with `publish: true`
  from `main` (the sandbox token can't push tags, so the workflow tags `v<manifest version>`
  itself, cuts the GitHub Release, and uploads to the store).
- **Designated dev branch:** the session harness assigns a per-session `claude/*` branch. Its PRs
  keep getting merged, so reset the branch from `origin/main` for each new change; force-with-lease
  is fine (it only ever carries already-merged history).
- **Store rejects a new upload while one is in review** (`ITEM_NOT_UPDATABLE`). Don't try to ship
  a new store version until the pending one clears.
- **Installer scripts must be pure ASCII.** Windows PowerShell 5.1 reads a UTF-8-no-BOM `.ps1` as
  Windows-1252, so an em-dash breaks parsing. `test/unit/installer.test.js` guards this.
- **Sideload-only build bits:** the `nativeMessaging` permission + a fixed manifest `key` are
  injected **only** into the sideload builds by `scripts/build.mjs`; the Chrome Web Store zip stays
  minimal (`["storage"]`, no key). A manifest test guards it.
- **MSI job** is best-effort (`continue-on-error`) and often fails — never blocks the release.
- **A user's exported log is only evidence of the build that produced it.** Check its `version`
  field before drawing any conclusion from it.

---

## Archive

Older state, kept for background only. **Nothing below is current** — §1 and §2 above are.

## Historical — state as of 2026-07-29 (post-0.4.6 ship)

> Superseded by the 2026-09-08 block above: 0.4.7/0.4.8/0.4.9 have all since published and the
> submission slot is OPEN. Kept for the 0.4.6 background only — do not read its "BLOCKED" and
> "latest release" lines as current.

- **Latest GitHub release:** **`v0.4.6` (2026-07-29)** — tag `v0.4.6` on `main` @ `92ff8f9`, cut by
  the Release workflow (`publish: true`) with all four prebuilt zips attached. The next dev cycle
  has begun: `manifest.json`/`package.json` are bumped to **0.4.7**.
- **Chrome Web Store:** **0.4.6 uploaded + auto-submitted 2026-07-29** (`webstore` job green; log:
  "Uploading feedhacker-0.4.6-store.zip… / Publishing… / Publish successful" with
  `CWS_AUTO_PUBLISH=true`). That means **submitted for Google review, NOT approved** — the live
  package is still 0.4.5 until the "Item successfully published (Version 0.4.6)" email lands. The
  submission slot is therefore **BLOCKED**.
  (The best-effort `msi` job failed again on the WiX gate and never blocks anything.)
- **What 0.4.6 fixes for users, once approved:**
  - The **`Fh` element-mark icon finally reaches installs.** The listing had shown it since
    2026-07-24, but the store's published *package* was still 0.4.5, so every install kept the old
    MAX "M". Root cause: listing assets and the uploaded package are two independent publish
    channels (`best_practices.md` §33).
  - **Windows auto-update, which was completely broken.** `Sync-LatestRelease` selected the release
    asset with a blacklist, so `feedhacker-<v>-store-submission.zip` (no `manifest.json`) won and
    both the daily task and "Update now" failed every run. Now an anchored allowlist (§34).
  - The installer **no longer falsely claims** it registered the daily update task (§35).
- **Guards added** (branch `claude/chrome-app-icon-mismatch-fkhy9a`, PR #55, squashed to `92ff8f9`):
  `test/unit/brand-assets.test.js` + `test/system/build.system.test.js` pin the store-listing icons
  *and* the icons inside the built package to one brand blue (`test/png.js` holds the constant), and
  `test/unit/installer-update.test.js` locks the updater's asset selection against the real
  four-asset release set. Both verified to fail when the original bugs are reintroduced. Six
  unreferenced pre-rebrand rasters were deleted from the repo root.
- **Review apparatus changed** (maintainer's call, 2026-07-29): **green CI is the merge gate** and
  self-review against `best_practices.md` before pushing is the review step. `.coderabbit.yaml` was
  deleted and stays deleted — it forced draft reviews and re-review-on-every-push, the usage we
  don't want to pay for. Then **CodeRabbit was removed from every repo except `max3` and
  `netsniff`**, so **no AI reviewer runs on FeedHacker at all**. (This flip-flopped three times in
  one session — `REVIEWERS_STATUS.md` records the sequence so it isn't re-litigated. Removed is
  current.) Prior CodeRabbit findings stay credited in `KNOWN_ISSUES.md`'s **Found by** column.
- **Store item ID:** `kccajfoghkplakndamlohpepopdpelkb` (moved to this new item as of 0.3.0;
  the old item was `djfbniehjjngpkimngegnjdeamfofnoa`).
- **Monitoring:** Google's "Item successfully published" email to newellnarco@gmail.com is the
  reliable notification. On a new session, re-run steps 2/3 above (search
  `from:chromewebstore-noreply@google.com newer_than:7d`, read the Version field of the newest
  "published" email).

## Shipped — v0.4.5 (2026-07-21)

- **Released** on GitHub (tag `v0.4.5`) and **uploaded to the Chrome Web Store** (auto-published →
  in review). Full change list under `## [0.4.5] — 2026-07-21` in `CHANGELOG.md`.
- **What shipped** (merged to `main` via PRs #42–#47, plus the `Fh` logo SVG):
  - **In-place "Update now" for Chrome Web Store installs** (#42) — store users who hit *Check for
    updates* can now fetch and apply a published update on the spot via Chrome's own update API
    (`requestUpdateCheck` → `runtime.reload`), then just refresh the LinkedIn tab — no browser
    restart. If the new version is on GitHub but not yet live on the store (still in Google review),
    it says so plainly instead of failing.
  - **Popup help moved behind a "?" button** (#43) — the two always-on help blurbs (the "How it
    works" line and the Aggression note) are gone from the default view; a small **?** by the Enable
    toggle reveals the same help in a popup on demand (close with ×, click-outside, or Esc). Also:
    the Aggression slider label now shows plainly **strict / balanced / aggressive** (the
    "(~28% hidden)" fraction overflowed the row and got clipped).
  - **Welcome-page refresh** (#44) — the pinning-guide puzzle-piece icon now uses Chrome's own
    monochrome gray "Extensions" glyph (was the colorful 🧩), so the step reads true to the real
    toolbar button.
  - **MAX3/netsniff engineering discipline adopted** (#45, #46) — CodeRabbit config, numbered
    `best_practices.md` rules (§19–29), a tree-integrity ledger, a test matrix, and CI hardening
    from applying the PR-review findings.
  - **v0.4.5 UI refresh + heartbeat paging fix + Advanced removal** (#47) — the "No LinkedIn post
    markers found" alarm no longer false-fires during LinkedIn paging (distinguishes a genuine
    selector break from an empty/loading feed).
  - **Scalable `Fh` logo** — a `feedhacker-logo.svg` app icon (FH-040).
- **Backlog / possible follow-ups:**
  - The best-effort **MSI** installer build still fails in CI (WiX `Build MSI` step); it's
    `continue-on-error` and never blocks a release, so it's optional to fix.
  - The `chrome-extension://invalid/` request some users see on LinkedIn is LinkedIn-side (their
    fetch interceptor hitting a stale reference after a context swap) — FeedHacker no longer
    contributes an enumerable resource to it (0.4.3). Nothing further actionable on our side.
